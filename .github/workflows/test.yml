name: Testing
on:
  push:
    branches: [main]
jobs:
  Scan:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello From Bash Shell"
      - run: print ("Hello From Python")
        shell: python
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.x"
  Build:
    runs-on: ubuntu-24.04
    needs: Scan
    steps:
      - name: Checking_Dependacy 
        run: echo "This Job is depends on Scan Job and is run on $(date)"
